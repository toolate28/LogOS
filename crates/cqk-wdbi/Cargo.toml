[package]
name = "cqk-wdbi"
version = "0.1.0"
edition = "2021"
description = "Weighted-distance braid index — Ripser++ proxy for persistent homology"
license = "MIT OR Apache-2.0"

[dependencies]
reson8-core = { path = "../core" }
reson8-topology = { path = "../reson8-topology" }
petgraph = "0.8"
serde = { version = "1", features = ["derive"] }
