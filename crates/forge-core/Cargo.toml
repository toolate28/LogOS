[package]
name = "reson8-forge-core"
version = "0.1.0"
edition = "2021"
description = "Forge protocol surface — re-export of reson8-core modules for TUI/triweave"

[dependencies]
reson8-core = { path = "../core" }