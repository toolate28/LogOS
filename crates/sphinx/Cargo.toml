[package]
name = "reson8-sphinx"
version = "0.1.0"
edition = "2021"
description = "SPHINX oracle — knowledge graph queries, causal inference, hypothesis generation"

[dependencies]
reson8-core = { path = "../core" }
reson8-wave = { path = "../wave" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
