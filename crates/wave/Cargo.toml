[package]
name = "reson8-wave"
version = "0.1.0"
edition = "2021"
description = "WAVE coherence scoring engine — Fibonacci-weighted composite health metric"

[dependencies]
reson8-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
