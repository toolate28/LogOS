[package]
name = "reson8-artifact-pipeline"
version = "0.1.0"
edition = "2021"
description = "Artifact pipeline — ATOM-tagged build, sign, publish workflow"

[dependencies]
reson8-core = { path = "../core" }
reson8-hash = { path = "../hash" }
reson8-wave = { path = "../wave" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
