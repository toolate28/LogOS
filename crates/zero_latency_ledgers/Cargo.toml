[package]
name = "reson8-zero-latency-ledgers"
version = "0.1.0"
edition = "2021"
description = "BQP-complete provenance ledger — Jones polynomial, NEAR testnet, trace_n_braid"

[dependencies]
reson8-core = { path = "../core" }
reson8-hash = { path = "../hash" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
