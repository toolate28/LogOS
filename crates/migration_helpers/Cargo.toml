[package]
name = "reson8-migration-helpers"
version = "0.1.0"
edition = "2021"
description = "Schema migration helpers for D1 and Bookshelf state evolution"

[dependencies]
reson8-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
