[package]
name = "reson8-topology"
version = "0.1.0"
edition = "2021"
description = "TDA engine — Vietoris-Rips, persistent homology, Betti numbers, barcodes"

[dependencies]
reson8-core = { path = "../core" }
reson8-wave = { path = "../wave" }
serde = { version = "1", features = ["derive"] }
ndarray = "0.17"
