[workspace]
members = [
    "crates/resonance-invariant",
    "crates/styx-vfs-layer",
    "crates/coherence-mcp",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
