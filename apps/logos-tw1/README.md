# logos-tw1

Firebase App Hosting backend for project **tri-weavon** (`us-east4`).

Lattice routes: `/agda` `/apps` `/crates` `/cutiles` `/docs` `/kernels`
`/lean` `/notebooks` `/ops` `/tools`

See `docs/ops/APPHOSTING-LOGOS-TW1-20260815.md`.
